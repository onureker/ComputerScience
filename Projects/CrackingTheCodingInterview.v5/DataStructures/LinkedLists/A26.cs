using System;
using Common;

namespace CrackingTheCodingInterview.v5.DataStructures.LinkedLists
{
    //Given a circular linked list, implement an algorithm which returns the node at the beginning of the loop.
    //DEFINITION
    //Circular linked list: A(corrupt) linked list in which a node's next pointer points to an earlier node, so asto makea loop in the linked list.
    //EXAMPLE
    //Input: A - > B - > C - > D - > E - > C [the same C as earlier]
    //Output:C
    public class A26: IProgram
    {
        public void Run(string[] args)
        {
            //TODO: Complete
            throw new NotImplementedException();
        }
    }
}
